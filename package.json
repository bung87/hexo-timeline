{
  "name": "hexo-timeline",
  "description": "",
  "version": "0.0.1",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && git push"
  },
  "dependencies": {
    "mdline": "^1.0.2",
    "mocha": "*"
  },
  "devDependencies": {}
}
